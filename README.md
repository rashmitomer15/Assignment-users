# Assignment-users
Microservices assignment

This project will need few env variables to be passed while image run:

${JAEGER_HOST} 

${JAEGER_PORT}

${MYSQLDB_URL}

${MYSQLDB_USERNAME}

${MYSQLDB_PASSWORD}
